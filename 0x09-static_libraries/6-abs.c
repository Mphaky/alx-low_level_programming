#include "main.h"

/**
 * _abs - computes the absolute value of an integar
 * @n: the int to check
 * Rey=turn: the absolute value of int 
 */
int -abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
